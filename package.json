{
  "name": "martin-co-kitchen-app",
  "version": "2.0.0",
  "description": "Single-file kitchen-operations app: ingredients as states with keep-lives, recipes as bipartite state/process graphs, a resource-constrained cook schedule driving shopping, inventory, waste and nutrition. Data lives in versioned JSON packs spliced into one deployable HTML file.",
  "private": true,
  "scripts": {
    "build": "python3 src/build.py",
    "test": "cd test && node test.js",
    "verify": "npm run build && npm test"
  },
  "license": "UNLICENSED"
}
